name: Sync Fork

on:
  workflow_dispatch: # on button click

jobs:
  sync:

    runs-on: ubuntu-latest

    steps:
      - uses: tgymnich/fork-sync@v1.8
        with:
          owner: raspberrypi
          base: pi-6.8.y
          head: pi-6.8.y
